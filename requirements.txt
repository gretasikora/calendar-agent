# Environment variables
python-dotenv

# Flask web framework
flask
flask-cors

# Agent Squad framework (OpenAI agents + classifier)
agent-squad[openai]

# OpenAI SDK (used by agent-squad OpenAI agents)
openai

# Required transitively by agent-squad: its retrievers package imports
# amazon_kb_retriever (and thus boto3) at import time, even when only the
# OpenAI agents are used.
boto3

# Date/time utilities for calendar scheduling
python-dateutil
pytz

# Async HTTP client (used to call the MCP server)
aiohttp
